import 'package:flutter/material.dart';

//TODO: create platform view
class HlsPlayerWidget extends StatelessWidget {
  const HlsPlayerWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
